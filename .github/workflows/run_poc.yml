name: Test Cron Run

on:
  schedule:
    - cron: '*/2 * * * *'  # 每5分钟触发一次（方便测试）
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: echo "✅ Cron job is running!"
